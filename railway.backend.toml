[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m marvin_ui.server"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"

[[volumes]]
mountPath = "/data"
