[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m uvicorn marvin_ui.server:app --host 0.0.0.0 --port ${PORT:-8095}"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"

[[volumes]]
mountPath = "/data"
